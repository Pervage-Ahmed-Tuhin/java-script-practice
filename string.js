let sentence ="I am a good boy";

let reverse = "";

for(const letter of sentence)
{
    //reverse=letter+reverse; // this gives the reverse form
    //reverse = reverse+letter; //this gives the normal form 
}

console.log(reverse);